---
title: Secret
---
<%
d = []
Dir.glob("source/diary/*/[01]*secret*").sort.reverse.each do |filename|
  header = YAML.load_file(filename)
  d << gen_link(filename, header['title'], true)
end
%>
<dl>
<%= d.join("\n") %>
</dl>
