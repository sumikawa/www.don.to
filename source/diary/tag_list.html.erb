---
layout: site
title: Tag list
---
<dt>
<%
  tags_list.keys.sort.each do |tag|
%>
  <dd><%= link_to(tag, "/diary/tag/#{tag.gsub(' ', '-')}") %> : <%= tags_list[tag].size %></dd>
<%
    end
%>
</dt>
